/// Conversation is currently represented by `Chat`.
///
/// This export is here so feature code can depend on `models/conversation/*`
/// while we migrate naming and structure gradually.
library;
export 'package:trulura/models/chat.dart';
